/**
 * Enumeration of all the types of notifications that can be published on the existing notification message queue.
 */
export enum NotificationType {
    SOME_NOTIFICATION = "SOME_NOTIFICATION_TYPE",
}
